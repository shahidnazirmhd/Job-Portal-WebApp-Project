<header>
    <div><a href="/">Fouz</a></div>
    <nav>
        <ul>
            <% if (locals.isAuth) { %>
                <li>
                    <form action="/logout" method="post">
                        <input type="hidden" value="<%= locals.csrfToken %>" name="_csrf">
                        <button>Logout</button>
                    </form>
                </li> 
            <% } %>
        </ul>
    </nav>
</header>